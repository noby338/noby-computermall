<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.woniuxy.financialmanage.dao.PurchaseOrderDao">

    <resultMap type="com.woniuxy.commonentity.entity.PurchaseOrder" id="PurchaseOrderMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="clientId" column="client_id" jdbcType="INTEGER"/>
        <result property="phoneNum" column="phone_num" jdbcType="INTEGER"/>
        <result property="address" column="address" jdbcType="INTEGER"/>
        <result property="totalMoney" column="total_money" jdbcType="NUMERIC"/>
        <result property="status" column="status" jdbcType="INTEGER"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="PurchaseOrderMap">
        select
          id, client_id, phone_num, address, total_money, status
        from purchase_order
        where id = #{id}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="PurchaseOrderMap">
        select
          id, client_id, phone_num, address, total_money, status
        from purchase_order
        <where>
            <if test="purchaseOrder.id != null">
                and id = #{purchaseOrder.id}
            </if>
            <if test="purchaseOrder.clientId != null">
                and client_id = #{purchaseOrder.clientId}
            </if>
            <if test="purchaseOrder.phoneNum != null">
                and phone_num = #{purchaseOrder.phoneNum}
            </if>
            <if test="purchaseOrder.address != null">
                and address = #{purchaseOrder.address}
            </if>
            <if test="purchaseOrder.totalMoney != null">
                and total_money = #{purchaseOrder.totalMoney}
            </if>
            <if test="purchaseOrder.status != null">
                and status = #{purchaseOrder.status}
            </if>
        </where>
        limit #{pageable.offset}, #{pageable.pageSize}
    </select>

    <!--统计总行数-->
    <select id="count" resultType="java.lang.Long">
        select count(1)
        from purchase_order
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="clientId != null">
                and client_id = #{clientId}
            </if>
            <if test="phoneNum != null">
                and phone_num = #{phoneNum}
            </if>
            <if test="address != null">
                and address = #{address}
            </if>
            <if test="totalMoney != null">
                and total_money = #{totalMoney}
            </if>
            <if test="status != null">
                and status = #{status}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into purchase_order(client_id, phone_num, address, total_money, status)
        values (#{clientId}, #{phoneNum}, #{address}, #{totalMoney}, #{status})
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update purchase_order
        <set>
            <if test="clientId != null">
                client_id = #{clientId},
            </if>
            <if test="phoneNum != null">
                phone_num = #{phoneNum},
            </if>
            <if test="address != null">
                address = #{address},
            </if>
            <if test="totalMoney != null">
                total_money = #{totalMoney},
            </if>
            <if test="status != null">
                status = #{status},
            </if>
        </set>
        where id = #{id}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete from purchase_order where id = #{id}
    </delete>

</mapper>

