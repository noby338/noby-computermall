<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.woniuxy.salesmanage.dao.SaleOrderItemDao">

    <resultMap type="com.woniuxy.commonentity.entity.SaleOrderItem" id="SaleOrderItemMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="num" column="num" jdbcType="INTEGER"/>
        <result property="saleOrderId" column="sale_order_id" jdbcType="INTEGER"/>
        <result property="modelId" column="model_id" jdbcType="INTEGER"/>
        <result property="salePrice" column="sale_price" jdbcType="NUMERIC"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="SaleOrderItemMap">
        select
          id, num, sale_order_id, model_id, sale_price
        from sale_order_item
        where id = #{id}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="SaleOrderItemMap">
        select
          id, num, sale_order_id, model_id, sale_price
        from sale_order_item
        <where>
            <if test="saleOrderItem.id != null">
                and id = #{saleOrderItem.id}
            </if>
            <if test="saleOrderItem.num != null">
                and num = #{saleOrderItem.num}
            </if>
            <if test="saleOrderItem.saleOrderId != null">
                and sale_order_id = #{saleOrderItem.saleOrderId}
            </if>
            <if test="saleOrderItem.modelId != null">
                and model_id = #{saleOrderItem.modelId}
            </if>
            <if test="saleOrderItem.salePrice != null">
                and sale_price = #{saleOrderItem.salePrice}
            </if>
        </where>
        limit #{pageable.offset}, #{pageable.pageSize}
    </select>

    <!--统计总行数-->
    <select id="count" resultType="java.lang.Long">
        select count(1)
        from sale_order_item
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="num != null">
                and num = #{num}
            </if>
            <if test="saleOrderId != null">
                and sale_order_id = #{saleOrderId}
            </if>
            <if test="modelId != null">
                and model_id = #{modelId}
            </if>
            <if test="salePrice != null">
                and sale_price = #{salePrice}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into sale_order_item(num, sale_order_id, model_id, sale_price)
        values (#{num}, #{saleOrderId}, #{modelId}, #{salePrice})
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update sale_order_item
        <set>
            <if test="num != null">
                num = #{num},
            </if>
            <if test="saleOrderId != null">
                sale_order_id = #{saleOrderId},
            </if>
            <if test="modelId != null">
                model_id = #{modelId},
            </if>
            <if test="salePrice != null">
                sale_price = #{salePrice},
            </if>
        </set>
        where id = #{id}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete from sale_order_item where id = #{id}
    </delete>

</mapper>

