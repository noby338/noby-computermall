server:
  port: ${port:2021}
logging:
  level:
    com.woniuxy.purchasemanage: debug
spring:
  application:
    name: purchase-manage
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/noby_computermall?serverTimezone=UTC
    username: root
    password: 123
  cloud:
    nacos:
      server-addr: localhost:8848

mybatis:
  type-aliases-package: com.woniuxy.purchasemanage.entity
  mapper-locations: classpath:/mapper/*Dao.xml


